{
    IBClasses = (
        {CLASS = BorderlessPanel; LANGUAGE = ObjC; SUPERCLASS = NSPanel; }, 
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = ImagePopupButton; LANGUAGE = ObjC; SUPERCLASS = NSPopUpButton; }, 
        {CLASS = LibraryCell; LANGUAGE = ObjC; SUPERCLASS = NSTextFieldCell; }, 
        {
            ACTIONS = {
                changeLibraryPreviewPanelSegmentedControl = id; 
                changeLibraryRowType = id; 
                importCurrent = id; 
                newFolder = id; 
                open = id; 
                openDefaultLibraryPath = id; 
                refreshItems = id; 
                removeSelectedItems = id; 
                renameItem = id; 
                saveAs = id; 
            }; 
            CLASS = LibraryController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                actionButton = ImagePopupButton; 
                exportAccessoryView = NSView; 
                exportOnlySelectedButton = NSButton; 
                importAccessoryView = NSView; 
                importCurrentButton = NSButton; 
                importOptionPopUpButton = NSPopUpButton; 
                libraryPreviewPanel = BorderlessPanel; 
                libraryPreviewPanelImageView = LibraryPreviewPanelImageView; 
                libraryPreviewPanelSegmentedControl = NSSegmentedControl; 
                libraryRowTypeSegmentedControl = NSSegmentedControl; 
                libraryTableView = LibraryTableView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {
            CLASS = LibraryPreviewPanelImageView; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSImageView; 
        }, 
        {
            ACTIONS = {copy = id; redo = id; undo = id; }; 
            CLASS = LibraryTableView; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSOutlineView; 
        }, 
        {CLASS = NSSegmentedControl; LANGUAGE = ObjC; SUPERCLASS = NSControl; }
    ); 
    IBVersion = 1; 
}